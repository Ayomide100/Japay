import React from 'react';
import Form from './pages/Form';

function Profile({data}) {
    return (
        <>
            <Form data={data}/>
        </>
    );
}

export default Profile;